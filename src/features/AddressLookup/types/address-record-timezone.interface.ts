export interface AddressRecordTimezone {
  readonly abbr: string;
  readonly current_time: string;
  readonly id: string;
  readonly is_dst: boolean;
  readonly offset: number;
  readonly utc: string;
}
